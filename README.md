# CROWDSEC-BOUNCERS-ASUSMERLIN


## Development

- Linting: `pre-commit` hooks configured in `.pre-commit-config.yaml`
- Type checking: `mypy` config in `mypy.ini`
- Docker: Build with `docker build -t darkero-scraper .`

